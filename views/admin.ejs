<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <title>Admin</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Saját stílusok -->
  <link rel="stylesheet" href="<%= basePath %>/css/main.css">
  <link rel="stylesheet" href="<%= basePath %>/css/sajat.css">
</head>
<body>
  <%- include('partials/header') %>

  <main class="container mt-5">
    <!-- Külső kártya: sötét háttér, világos szöveg -->
    <div class="card shadow-lg rounded-4 admin-panel bg-dark text-light">
      <div class="card-body">
        <h2 class="mb-3 text-light">Admin felület</h2>
        <p class="mb-4 text-light">
          Ehhez a menüponthoz csak az admin fér hozzá. Az alábbi összesítők az oldal fontosabb adatait mutatják.
        </p>

        <div class="row g-4">
          <!-- Felhasználók -->
          <div class="col-md-4">
            <div class="card stat-card h-100 bg-secondary text-light border-0 shadow-sm">
              <div class="card-body text-center">
                <h5 class="card-title text-uppercase small text-warning mb-2">Felhasználók</h5>
                <p class="display-4 fw-bold mb-0"><%= stats.users %></p>
              </div>
            </div>
          </div>

          <!-- Üzenetek -->
          <div class="col-md-4">
            <div class="card stat-card h-100 bg-secondary text-light border-0 shadow-sm">
              <div class="card-body text-center">
                <h5 class="card-title text-uppercase small text-warning mb-2">Kapott üzenetek</h5>
                <p class="display-4 fw-bold mb-0"><%= stats.messages %></p>
              </div>
            </div>
          </div>

          <!-- Ételek -->
          <div class="col-md-4">
            <div class="card stat-card h-100 bg-secondary text-light border-0 shadow-sm">
              <div class="card-body text-center">
                <h5 class="card-title text-uppercase small text-warning mb-2">Ételek</h5>
                <p class="display-4 fw-bold mb-0"><%= stats.etelek %></p>
              </div>
            </div>
          </div>
        </div>

        <hr class="my-4 border-light">

        <p class="mb-0 text-light-50">
          Tipp: a részletes adatok a felső menüből is elérhetők (Receptek, Üzenetek, CRUD).
        </p>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
