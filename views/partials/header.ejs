<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
  <div class="container">
    <!-- Logo / Bal felső sarok -->
    <a class="navbar-brand fw-bold" href="<%= basePath %>/">
      🍽️ Receptek
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Menüsor -->
    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav ms-auto">
        <!-- Bal oldali fő menüpontok -->
        <li class="nav-item">
          <a class="nav-link <%= title === 'Főoldal' ? 'active' : '' %>" href="<%= basePath %>/">Főoldal</a>
        </li>

        <li class="nav-item">
          <a class="nav-link <%= title === 'Receptek' ? 'active' : '' %>" href="<%= basePath %>/receptek">Receptek</a>
        </li>

        <li class="nav-item">
          <a class="nav-link <%= title === 'Kapcsolat' ? 'active' : '' %>" href="<%= basePath %>/kapcsolat">Kapcsolat</a>
        </li>

        <% if (user) { %>
          <li class="nav-item">
            <a class="nav-link <%= title === 'Üzenetek' ? 'active' : '' %>" href="<%= basePath %>/uzenetek">Üzenetek</a>
          </li>
        <% } %>

        <li class="nav-item">
          <a class="nav-link <%= title === 'CRUD' ? 'active' : '' %>" href="<%= basePath %>/crud">CRUD</a>
        </li>

        <!-- Jobb oldal: auth blokk -->
        <% if (!user) { %>
          <li class="nav-item">
            <a class="nav-link <%= title === 'Regisztráció' ? 'active' : '' %>" href="<%= basePath %>/regisztracio">Regisztráció</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= title === 'Bejelentkezés' ? 'active' : '' %>" href="<%= basePath %>/login">Bejelentkezés</a>
          </li>
        <% } else { %>
          <% if (user.szerep === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link <%= title === 'Admin' ? 'active' : '' %>" href="<%= basePath %>/admin">Admin</a>
            </li>
          <% } %>

          <!-- Kijelentkezés + felhasználónév közös kapszulában -->
          <li class="nav-item ms-2">
            <div class="d-flex align-items-center border border-warning rounded-pill px-3 py-1">
              <a class="nav-link p-0 me-2" href="<%= basePath %>/logout">
                Kijelentkezés
              </a>
              <span class="fw-bold text-warning">
                - <%= user.nev %><% if (user.szerep === 'admin') { %> (admin)<% } %>
              </span>
            </div>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
